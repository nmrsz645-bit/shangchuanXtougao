# 上传 + 投稿中心：接手约定

- 只修改 `E:\自动化\上传+投稿`；绝不修改 `E:\自动化\api投稿2.0` 与 `E:\自动化\自动上传\程序`。
- 不提交、上传、清空或覆盖飞书设置、Token、Chrome 数据、`个人数据`、`API投稿2.0\config`、`data`、`logs`、发布版和 `program.previous-*`。
- 测试必须离线模拟外部接口；未获用户明确授权，不执行真实投稿、上传、浏览器自动化、构建或在线发布。
- 修改投稿逻辑后先运行 API 投稿测试；涉及交接时同步更新 `TIMEOFF.md`，并在提交前检查暂存文件和敏感信息。
